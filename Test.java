public class Test {
    public static void main(String[] args) {
        /* a. Changed Test to test
         * Prediction: runtime error
         * Error type (if any): compile
         * Error message from the compiler (if any): Error:(1, 8) java: class test is public, should be declared in a file named test.java
         */


        /**
         * Overview: Examine the three types of errors encountered in Java
         Programs
         * Input: None
         * Output: Text Display * Variables: None
         * Plan:
         * 1. User runs the program
         * 2. An error should display based on the introduced errors
         *
         * @version: 1.0 September 08, 2020 (today's date)
         */

        System.out.println("An Emergency Broadcast");
        /* b. Changed Emergency to emergency
         * Prediction: nothing happens
         * Error type (if any): none
         * Error message from the compiler (if any):
         */

        /* c. remove the first quotation mark in the string
         * Prediction: compile error
         * Error type (if any): compile-time error
         * Error message from the compiler (if any): unclosed string literal
         */

        /* d. remove the last quotation mark in the string
         * Prediction: compile error
         * Error type (if any): compile-time error
         * Error message from the compiler (if any): unclosed string literal
         */

        /* e. change main to man
         * Prediction: runtime error
         * Error type (if any): run-time error
         * Error message from the compiler (if any):
         */

        /* f. change println to bogus
         * Prediction: compile-time error
         * Error type (if any): compile-time error
         * Error message from the compiler (if any): bogus is not a method
         */

        /* f. remove the semicolon at the end of the println statement
         * Prediction: compile-time error
         * Error type (if any): compile-time error
         * Error message from the compiler (if any): ; expected
         */

        /* f. remove the last brace in the program
         * Prediction: compile-time error
         * Error type (if any): compile-time error
         * Error message from the compiler (if any):
         */

    }
}
